# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfplan

# SSH keys (private keys must never be committed)
*.pem
terra-key-ansible.pem
# Private key used by the lab (public key .pub is safe to commit)
terraform/terra-key-ansible
~/keys/terra-key-ansible

# Ansible
*.retry
.vault_password
.vault_pass*.txt
modules/06-vault/vars/secrets.yml

# Auto-generated inventories (regenerated by terraform apply)
inventories/dev/hosts.ini
inventories/dev/bootstrap.ini

# Trainer notes (personal, not for learners)
notes/

# OS files
.DS_Store
.claude/
terraform/keys/terra-key-ansible